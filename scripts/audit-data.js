const fs=require('fs');const p='ket_qua_1500_phim.json';if(!fs.existsSync(p))throw new Error('missing '+p);const a=require('../'+p);if(!Array.isArray(a)||!a.length)throw new Error('invalid movie database');const withStreams=a.filter(m=>Array.isArray(m.streams)&&m.streams.some(s=>s&&s.url)).length;console.log('[AUDIT] movies='+a.length+' withStreams='+withStreams);if(withStreams<1)process.exit(1);